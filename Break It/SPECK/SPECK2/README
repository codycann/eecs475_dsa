////////////
// README //
////////////

Compile with make command.

HOW TO USE:
Format is similar to that of Piazza post @310.

$ ./program keygen
     key_part_1 key_part_0
     
$ ./program encrypt key_part_1 key_part_0 plaintext_part_1 plaintext_part_0
     ciphertext_part_1 ciphertext_part_0
     
$ ./program decrypt key_part_1 key_part_0 ciphertext_part_1 ciphertext_part_0
     plaintext_part_1 plaintext_part0

Be sure to use the same key generated by the keygen!

////////////////////////////////////////////////////////////////////////

Changelog

// rev2.4 //
done. ez.

// rev2.3 //
working decrypt(), keyGen(). Need to parse cmd line.

// rev2.2 //
attempts at decryption function. problem is with unsigned subtraction
-> unsigned negative = 0

// rev2.1 //
Working keyExpand() and encrypt(). Need additional test to see if breaks.
Still need decryption (Inverse of XOR is XOR)